name: custom_window
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.12.0-316.0.dev

dependencies:
  flutter:
    sdk: flutter
  native_toolchain_ninja: ^0.1.1
  hooks: ^1.0.2
  code_assets: ^1.0.0
  logging: ^1.3.0
  ffi: ^2.2.0
  win32: ^6.0.0
  headless_widgets: ^0.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  ffigen: ^20.1.1

flutter:
  uses-material-design: true
